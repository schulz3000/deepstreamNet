namespace DeepStreamNet.Tests.Helper
{
    static class TestConstants
    {
        public const string ServerCommunication = nameof(ServerCommunication);
        public const string Local = nameof(Local);
    }
}
